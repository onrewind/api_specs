title: "Squad"
properties:
  id:
    type: string
    format: uuid
    readOnly: true
  name:
    type: string
    description: Squad's name.
  meta:
    type: object
    description: Squad's meta data.
  TeamId:
    type: string
    format: uuid
    description: team's id when creating a TeamSquad.
  SeasonId:
    type: string
    format: uuid
    description: season's id when creating a TeamSquad.

