post:
  tags:
    - Main API - Conference player
  summary: Process a live conference's switch to 'replay' state
  description: Process a live conference's switch to 'replay' state
  parameters:
    - $ref: '#/components/parameters/id'
  requestBody:
    description: id of the conference player
    required: true
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/BusinessPlayer'
  responses:
    '200':
      description: The user conference player created
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Video'
    '400':
      $ref: '#/components/responses/InvalidModel'
    '403':
      $ref: '#/components/responses/UserNotAuthorized'